For install macOS on hyperV

https://teams.microsoft.com/l/message/19:e7e274e58dd14f2ca34c282505d35fd7@thread.v2/1737477228367?context=%7B%22contextType%22%3A%22chat%22%7D

FOR MacOS Ninjar agent
https://ca.ninjarmm.com/agent/installer/b2f63a83-fa3b-479d-adf3-3dfe75f15fd1/ewolfpromainoffice-7.0.2317-installer.dmg


【For Macbook enrollment to Microsoft Defender】

https://learn.microsoft.com/en-us/defender-endpoint/mac-install-manually 



1000BASE- LX/LH is supported on links up to 10km, and 1000Base-ZX operates over links up to 70 km

three-tier architecture model   access ג€" distribution ג€" core ג€" distribution ג€" access

two pieces of information about a Cisco device can Cisco Discovery Protocol communicate     the native VLAN   the VTP domain

After you deploy a new WLAN controller on your network, which two additional tasks should you consider?   deploy load balancers   con¬ gure additional security policies

What is a function of Wireless LAN Controller   send LWAPP packets to access points

Which technology is used to improve web tra® c performance by proxy caching?    WSA(Web security application)

Which protocol does an access point use to draw power from a connected switch?   Cisco Discovery Protocol  Power Over Ethernet (PoE)

restrict the requests and force the user to wait 10 ms to retry an association request?    Enable the Protected Management Frame service and set the Comeback timer to 10

travel between ­ oors or to other areas in the building.       Enable Fast Transition and select the FT PSK option

What is a characteristic of frame switching?        performs a lookup to learn the destination interface

a router do when it is con¬ gured with the default DNS lookup settings, and a URL is entered on the CLI?   It attempts to query a DNS server on the network.

 on the WLC when implementing a RADIUS server for wireless authentication?      AAA Override and the IP address of the server

Down Down means no cable or wrong cable has been used.

FTP               It uses two separate connections for control and data tra® c.

encryption in wireless networks            uses protocols such as TKIP and CCMP to secure data

TFTP provide                                  loads con¬ guration files on systems without data storage devices

REST API use to communicate      HTTP

h interface on the WLC is limited to one when LAG is in use      . AP-manager

What is used to identify spurious DHCP servers?     DHCPDISCOVER

access point to forward all client tra® c through a wireless controller.          local

Which plane is centralized in software-de¬ ned networking?        control

How is a con¬ guration change made to a wireless AP in lightweight mode?           CAPWAP/LWAPP connection via the parent WLC

Which 802.11 management frame type is sent when a client roams between access points on the same SSID?          Reassociation Request


What is the recommended switch load-balancing mode for Cisco WLCs?   source-destination IP address

Ansible inventory           file that defines the target devices upon which commands and tasks are executed

northbound REST API for SDN?     application-facing interface for GET, POST, PUT, and DELETE methods

Which function generally performed by a traditional network device is replaced by a software-de¬ ned controller?               building route tables and updating the forwarding table

Which two northbound APIs are found in a software-de¬ ned network?              REST             SOAP


PUT            It replaces data at the destination.


How does Chef con¬ guration management enforce a required device con¬ guration?                 . The installed agent on the device connects to the Chef Infra Server and pulls its required con¬ guration from the cookbook.

Which bene¬ t does Cisco DNA Center provide over traditional campus management?                   Cisco DNA Center leverages APIs, and traditional campus management requires manual data gathering.


Which WPA mode uses PSK authenticaton                                                            . Personal

Which security method is used to prevent man-in-the-middle attacks?                               anti-replay

What is a characteristic of RSA                                                                   It is a public-key cryptosystem.


multiple branch offices and large-scale deployments?                                              GETVPN                             DMVPN       

What is an enhancement implemented in WPA3?                                                       defends against deauthentication and disassociation attacks


RSA            asymmetric encryption algorithm

two protocols within the IPsec suite?                 . ESP          . AH

WPA1           TKIP


802.1q double-tagging VLAN-hopping attack = configure the native VLAN with a nondefault VLAN ID
MAC flooding attack = configure 802.1x authenticate
man-in-the-middle spoofing attack = configure DHCP snooping
switch-spoofing VLAN-hopping attack = disable DTP


 server types support domain name to IP address resolution?            web         resolver

two actions are taken as the result of tra® c policing?                dropping       remarking

Which DSCP per-hop forwarding behavior is divided into subclasses based on drop probability?              . assured


What is the role of community strings in SNMP operations                     It serves as a password to protect access to MIB objects


What is the functionality of the Cisco DNA Center?                software-defined controller for automation of devices and services

OM3 and OM4 and OM4 fiber optic cable                             Both have a 50 micron core diameter.

signal frequency appears 60 times per minute                     1 Hz signal

link-local all-nodes IPv6 multicast address                      well-known link-local messages would start with “FF02”

used to interconnect one switch using 1000 BASE-SX GBIC modules and another switch using 1000 BASE-SX SFP  modules                           LC to SC

controller in a software-de¬ ned network                         setting packet-handling policies

With REST API, which standard HTTP header tells a server which media type is expected by the client?                        Accept: application/json

Which HTTP status code is returned after a successful REST API request?                                           200

ANSIBLE:
Uses SSH
Uses YAML
CHEF:
Uses Ruby
Uses TCP 10002
PUPPET:
Funtamental configuration stored in a manifest
Uses TCP 8140

Which de¬ nition describes JWT in regard to REST API security?                   an encoded JSON token that is used for authentication

Create new data structures and variables-> Create = POST
Read (retrieve) variable names, structures, and values -> Read = GET
Update or replace values of some variable Update -> = PATCH, PUT
Delete some variables and data structures Delete -> = DELETE

the function of `off-the-shelf` switches in a controller-based network                       forwarding packets


communication between an SDN controller end applications running over the network            REST API

script paradigm does Puppet use                   manifests and modules

function of the controller in a software-de¬ ned network?                making routing decisions

o REST API status-code classes
1xx-informational, the request was received.
2xx-Successful, request successfully received.
3xx-Redirection, further action need to be taken.
4xx-Client error, syntax error.
5xx-Server error, server failed.


two southbound APIs?              NETCONF          OpenFlow

plane handles switching for tra® c through a Cisco router             data  

Ansible        . playbook       task

device is added to Cisco DNA Center?            Managed state.      Provisioned state.

control-plane          is centralized by an SDN controller

control layer and the infrastructure layer          is the interface between the control plane and data plane   within the software-de¬ ned architecture

Software Defined Access (SDA) to provide a tunnel between two edge nodes in different fabrics           Virtual Extensible LAN (VXLAN

JSON data type is an unordered set of attribute-value pairs     object

HTTP messages to transfer data to applications r     REST    GET, PUT, POST and DELETE

southbound API            OpenFlow

Ansible:
- uses SSH (port 22) for remote device communication
- uses YAML for fundamental configuration
Chef:
- uses TCP port 10002 for configuration push jobs
- uses Ruby for fundamental configuration elements
Puppet:
- uses TCP 8140 for communication
- fundamental configuration elements are stored in a manifest

Puppet => arrows

structured data that includes arrays      JSON

API allows SDN controllers to dynamically make changes to the network        southbound API

two encoding methods are supported by REST APIs         XML          JSON

o prevent users from adding `Cisco` as a password        . password complexity enable

VLAN hopping attacks?     access ports to VLANs other than the native VLAN

WPA3-Personal security with a PSK           CCMP128

Install a trusted third-party certi¬ cate on the Cisco ISE.

DHCP snooping == Rogue server that spoofs IP configuration
Dynamic ARP Inspection == Cache poisoning
IP Source Guard == Rogue clients on the network
Storm control == Flood attacks

highest encryption and authentication using domain credentials from LDAP                                  Select WPA+WPA2 on Layer 2 Security.           Select 802.1X from under Authentication Key Management.

control panel SDN  decision routing   
data panel         forwording routing

WPA2 PSK        ASCII and Hex 

WPA        TKIP/MIC encryption

comprise part of a PKI              one or more CRLs        CA that grants certi¬ cates

802.11w             PMF to Required.

802.11r.            Fast Transition option to Enable and enable FT 802.1X under Authentication Key Management.

minimum number of characters that is required in ASCII format     8

1.Configure the native VLAN with a nondefault VLAN ID   802.q vlan-hopping attack
2. Configure 802.1x authenticate                        MAC flooding attack 
3. Configure DHCP snooping                              MITM spoofing
4. Disable DTP                                          switch-snooping vlan-hopping attack




STP goes from blocked, listening, learning, and forwarding
and RSTP（802.1w) goes from Discarding, Learning, and Forwarding

*** OSPF
R1(config)# router ospf 33
R1(config-router)# router-id 192.168.1.1
R1(config)# interface GigabitEthernet0/0      # Interface to R2
R1(config-if)# ip ospf 33 area 0            # Enable OSPF on this interface for Area 0
R1(config-if)# ip ospf priority 255         # Set OSPF priority to 255 (high priority for DR)

R1(config)# interface GigabitEthernet0/1      # Interface to R3
R1(config-if)# ip ospf 33 area 0            # Enable OSPF on this interface for Area 0
R1(config-if)# ip ospf priority 255         # Set OSPF priority to 255 (high priority for DR)
R1# clear ip ospf process
R1# wr

*** VLAN SWITCHPORT VOICE
interface e0/1
switchport mode access
switchport access vlan 88
switchport voice vlan 188

interface e0/2
switchport mode access
switchport voice vlan 177
wr

SW1 (config)# lldp run
wr


What is the RFC 4627 default encoding for JSON text?      UTF-8

*** DHCP on ROUTER

R1(config)# ip dhcp excluded-address 192.168.10.1 192.168.10.10
R1(config)# ip dhcp pool VLAN10_Pool
R1(dhcp-config)# network 192.168.10.0 255.255.255.0
R1(dhcp-config)# default-router 192.168.10.1
R1(dhcp-config)# dns-server 8.8.8.8 8.8.4.4
R1(dhcp-config)# lease 7 12 0  # 7 days, 12 hours lease time
R1(config)# service dhcp

username tech12 privilege 15 secret load1key
line vty 0-4
transport input telnet
Task 2 1918 RFC
Class A 10.0.0.0/8 255.0.0.0
Class B 172.16.0.0/12 255.240.0.0
ip access-list standard ISP_ACL
deny 10.0.0.0 0.255.255.255
deny 172.16.0.0 0.15.255.255
permit any
interface e0/1 ip access-group ISP_ACL out

*** IP ARP
ip arp inspection vlan 5
ip arp validate des-mac
ip arp validate src-mac
ip arp validate ip



















